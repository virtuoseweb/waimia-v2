---
// Page · /manifesto · Direction B · port de virtuoseweb/waimia src/components/directions/DirectionB.tsx
// (legacy repo cloné sous /Users/simonberos/waimia-site/existing-repo/ pour référence)
// Pattern : composition Astro (Pattern 1) + 1 React island (Pattern 4 · LiquidHero canvas RAF)
// Pacing :
//   1. Hero LiquidHero canvas (terracotta blobs + grain + parallax mouse)
//   2. Dark marquee (logos clients massifs Instrument Serif)
//   3. Acte I dark · stats massives 43 · <9 · ×4
//   4. Acte II asymétriques · services alterne gauche/droite
//   5. Manifeste accent terracotta « On refuse... »
//   6. Acte III dark · console VirtuoseOS XL
//   7. Acte IV cards en grille décalée
//   8. Writing dark
//   9. Acte V « Stop rehearsing. Ship. »

import Base from '../layouts/Base.astro';
import LiquidHero from '../components/motion/LiquidHero.tsx';
import ManifestoAccent from '../components/sections/ManifestoAccent.astro';
import Button from '../components/ui/atoms/Button.astro';
import Kicker from '../components/ui/atoms/Kicker.astro';
import { CLIENT_LOGOS, SIX_SERVICES, CASE_FEED, FIELD_NOTES } from '../data/sitemap';
import { langFromPath, t as ft, localizeHref as flh } from '../lib/i18n';

const lang = langFromPath(Astro.url.pathname);

const STATS = [
  { n:'43',  text: { en: 'tools in the average RevOps stack.',                      fr: 'outils dans la stack RevOps moyenne.' } },
  { n:'< 9', text: { en: 'touch the same customer record.',                         fr: 'touchent le même enregistrement client.' } },
  { n:'× 4', text: { en: 'success rate for agents shipped in 6 weeks vs 6 months.', fr: 'de réussite pour les agents livrés en 6 semaines vs 6 mois.' } },
];

const tripled = [...CLIENT_LOGOS, ...CLIENT_LOGOS, ...CLIENT_LOGOS];
---
<Base
  title={lang === 'fr' ? "Waimia · Manifesto · Construire du revenue, pas des démos" : 'Waimia · Manifesto · Build revenue, not demos'}
  description={lang === 'fr'
    ? "Direction B · Variante Manifesto. Hero canvas terracotta + services asymétriques + manifeste full-bleed. Pour les équipes qui livrent."
    : 'Direction B · Manifesto variant. Terracotta canvas hero + asymmetric services + full-bleed manifesto. For teams that ship.'}
>
  {/* HERO · LiquidHero canvas */}
  <section style="position:relative;min-height:100vh;overflow:hidden;background:var(--ink)">
    <LiquidHero client:visible />
    <div style="position:relative;z-index:2;padding:160px clamp(20px,4vw,48px) 80px;color:var(--paper);min-height:100vh;display:flex;flex-direction:column;justify-content:space-between">
      {/* top meta */}
      <div style="display:flex;justify-content:space-between;align-items:baseline;font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.55);flex-wrap:wrap;gap:14px">
        <span>Waimia · Manifeste 02</span>
        <span>Paris · 2026</span>
        <span><span style="color:var(--accent)">◉</span> Booking T3</span>
      </div>

      {/* headline */}
      <div style="max-width:1600px">
        <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.55);margin-bottom:24px">
          {lang === 'fr' ? (<>Une agence IA · <span style="color:var(--accent)">artisanat français, finition internationale</span></>) : (<>An AI agency · <span style="color:var(--accent)">French craft, international polish</span></>)}
        </div>
        <h1 style="font-family:var(--font-display);font-size:clamp(64px,13vw,220px);line-height:.88;letter-spacing:-0.035em;color:var(--paper);text-wrap:balance;font-weight:400;margin:0">
          {lang === 'fr' ? (
            <>Construire du <i style="color:var(--accent)">revenue</i>,<br />pas des <i>démos</i>.</>
          ) : (
            <>Build <i style="color:var(--accent)">revenue</i>,<br />not <i>demos</i>.</>
          )}
        </h1>
      </div>

      {/* bottom CTAs */}
      <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:48px;align-items:end;max-width:1600px">
        <p style="font-family:var(--font-display);font-size:clamp(15px,1.4vw,22px);line-height:1.4;color:rgba(246,241,232,.85);max-width:380px;margin:0">
          {lang === 'fr'
            ? "Une agence IA pour SaaS, fintech et industrie dont le moteur revenue doit livrer — pas répéter."
            : 'An AI agency for SaaS, fintech and industry that want their revenue engine to ship — not to rehearse.'}
        </p>
        <div style="display:flex;gap:10px;flex-wrap:wrap">
          <a href={flh('/contact', lang)} style="background:var(--accent);color:var(--paper);padding:16px 26px;border-radius:999px;font-size:13.5px;font-weight:500" data-mag="0.18">
            {lang === 'fr' ? 'Prendre RDV →' : 'Book a session →'}
          </a>
          <a href={flh('/console', lang)} style="border:1px solid rgba(246,241,232,.35);color:var(--paper);padding:16px 26px;border-radius:999px;font-size:13.5px">
            {lang === 'fr' ? 'La plateforme' : 'The platform'}
          </a>
        </div>
        <div style="font-family:var(--font-mono);font-size:11px;color:rgba(246,241,232,.55);letter-spacing:.08em;text-align:right">
          <div>SCROLL TO BEGIN ↓</div>
          <div style="margin-top:6px">FIVE ACTS · ONE DOCUMENT</div>
        </div>
      </div>
    </div>
  </section>

  {/* DARK MARQUEE · logos massifs */}
  <div style="background:var(--ink);color:var(--paper);border-top:1px solid rgba(246,241,232,.12);border-bottom:1px solid rgba(246,241,232,.12);padding:24px 0;overflow:hidden;white-space:nowrap">
    <div class="b-marquee-track">
      {tripled.map((logo) => (
        <>
          <span style="font-family:var(--font-display);font-size:clamp(24px,2.8vw,36px);padding:0 30px;color:var(--paper)">{logo}</span>
          <span style="padding:0 4px;color:var(--accent);font-size:24px;align-self:center">◦</span>
        </>
      ))}
    </div>
  </div>

  {/* ACTE I · stats dark */}
  <section style="padding:clamp(140px,16vw,200px) clamp(20px,4vw,48px);background:var(--ink);color:var(--paper)">
    <div style="max-width:1600px;margin:0 auto">
      <div style="display:grid;grid-template-columns:1fr 2fr;gap:clamp(40px,6vw,80px);margin-bottom:clamp(80px,10vw,120px)">
        <div style="position:sticky;top:120px;align-self:start">
          <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.5)">
            <span style="color:var(--accent)">I.</span> {lang === 'fr' ? 'Le constat' : 'The reckoning'}
          </div>
          <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.3);margin-top:4px">
            {lang === 'fr' ? 'Acte I' : 'Act I'}
          </div>
        </div>
        <p class="reveal" style="font-family:var(--font-display);font-size:clamp(40px,6.5vw,104px);line-height:1.02;letter-spacing:-0.025em;color:var(--paper);text-wrap:balance;font-weight:400">
          {lang === 'fr' ? (
            <>L'IA est <i style="color:var(--accent)">partout</i> et nulle part. Les stacks grossissent, les pipelines s'aplatissent. Le goulot n'est <i>jamais</i> le modèle.</>
          ) : (
            <>AI is <i style="color:var(--accent)">everywhere</i> and nowhere. Stacks grow, pipelines flatten. The bottleneck is <i>never</i> the model.</>
          )}
        </p>
      </div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:48px;padding-top:80px;border-top:1px solid rgba(246,241,232,.12)">
        {STATS.map((r) => (
          <div class="reveal">
            <div style="font-family:var(--font-display);font-size:clamp(72px,10vw,176px);line-height:.9;letter-spacing:-0.03em;color:var(--accent)">{r.n}</div>
            <p style="margin-top:18px;font-size:17px;line-height:1.5;color:rgba(246,241,232,.7);max-width:280px;font-family:var(--font-display)">
              {ft(r.text, lang)}
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* ACTE II · services asymétriques */}
  <section style="padding:clamp(140px,16vw,200px) clamp(20px,4vw,48px);background:var(--paper);color:var(--ink)">
    <div style="max-width:1600px;margin:0 auto">
      <div style="display:flex;justify-content:space-between;align-items:baseline;margin-bottom:clamp(60px,8vw,100px);flex-wrap:wrap;gap:14px">
        <Kicker>
          <span style="color:var(--accent)">II.</span> {lang === 'fr' ? 'La méthode' : 'The method'}
        </Kicker>
        <Kicker>6 · ALL IN HOUSE</Kicker>
      </div>
      <h2 class="reveal" style="font-family:var(--font-display);font-size:clamp(48px,8vw,148px);line-height:.94;letter-spacing:-0.03em;max-width:1400px;margin-bottom:clamp(80px,10vw,120px);text-wrap:balance;font-weight:400">
        {lang === 'fr' ? (
          <>On fait la couche <i style="color:var(--accent)">sans relief</i>. Exprès.</>
        ) : (
          <>We do the <i style="color:var(--accent)">unglamorous</i> layer. On purpose.</>
        )}
      </h2>
      {SIX_SERVICES.map((s, i) => {
        const reverse = i % 2 === 1;
        return (
          <div
            class="reveal b-service-row"
            data-reverse={reverse ? 'true' : 'false'}
          >
            {!reverse ? (
              <>
                <div class="b-svc-num">{s.k}</div>
                <div class="b-svc-name">{ft(s.label, lang)}</div>
                <p class="b-svc-body">{ft(s.body, lang)}</p>
                <div class="b-svc-arr">→</div>
              </>
            ) : (
              <>
                <div class="b-svc-arr-l">←</div>
                <p class="b-svc-body-r">{ft(s.body, lang)}</p>
                <div class="b-svc-name-r">{ft(s.label, lang)}</div>
                <div class="b-svc-num-r">{s.k}</div>
              </>
            )}
          </div>
        );
      })}
    </div>
  </section>

  {/* MANIFESTO ACCENT · réutilisé */}
  <ManifestoAccent />

  {/* ACTE III · CTA simplifié vers /console pour la console XL */}
  <section style="padding:clamp(140px,16vw,200px) clamp(20px,4vw,48px);background:var(--ink);color:var(--paper);text-align:center">
    <div class="signature-grid on-dark"></div>
    <div style="max-width:1100px;margin:0 auto;position:relative;z-index:1">
      <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.5);margin-bottom:32px">
        <span style="color:var(--accent)">III.</span> {lang === 'fr' ? 'La plateforme' : 'The platform'}
      </div>
      <h2 class="reveal" style="font-family:var(--font-display);font-size:clamp(48px,8vw,128px);line-height:.94;letter-spacing:-0.03em;color:var(--paper);text-wrap:balance;font-weight:400;margin-bottom:32px">
        {lang === 'fr' ? (
          <>Virtuose<i style="color:var(--accent)">OS</i>.<br />Le <i>kernel</i> sous chaque intervention.</>
        ) : (
          <>Virtuose<i style="color:var(--accent)">OS</i>.<br />The <i>kernel</i> under every engagement.</>
        )}
      </h2>
      <a href={flh('/console', lang)} style="display:inline-flex;align-items:center;gap:8px;background:var(--accent);color:var(--paper);padding:18px 32px;border-radius:999px;font-size:14px;font-weight:500;font-family:var(--font-mono)" data-mag="0.18">
        ▸ {lang === 'fr' ? 'Voir la Console (Direction C)' : 'See the Console (Direction C)'}
      </a>
    </div>
  </section>

  {/* ACTE IV · cards décalées */}
  <section style="padding:clamp(140px,16vw,200px) clamp(20px,4vw,48px);background:var(--paper);color:var(--ink)">
    <div style="max-width:1600px;margin:0 auto">
      <Kicker class="manifesto-acte-iv">
        <span style="color:var(--accent)">IV.</span> {lang === 'fr' ? 'Les preuves' : 'The proof'}
      </Kicker>
      <h2 class="reveal" style="font-family:var(--font-display);font-size:clamp(48px,8vw,148px);line-height:.92;letter-spacing:-0.03em;max-width:1400px;margin-bottom:clamp(60px,8vw,100px);text-wrap:balance;font-weight:400">
        {lang === 'fr' ? (<>Pas des démos. Des <i style="color:var(--accent)">résultats</i>.</>) : (<>Not demos. <i style="color:var(--accent)">Results</i>.</>)}
      </h2>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:32px">
        {CASE_FEED.slice(0, 4).map((c, i) => (
          <a class="reveal lift b-case-card" style={`margin-top:${i % 2 === 1 ? '60px' : '0'}`} href={flh(`/ressources/cas/${c.slug}`, lang)}>
            <div style="display:flex;justify-content:space-between;align-items:baseline">
              <div style="font-family:var(--font-mono);font-size:11px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase">CASE 0{i + 1} · {ft(c.sector, lang)}</div>
              <div style="font-family:var(--font-mono);font-size:11px;color:var(--muted)">{c.duration}</div>
            </div>
            <div style="font-family:var(--font-display);font-size:clamp(36px,5vw,72px);line-height:.95;letter-spacing:-0.025em;margin-top:18px">{c.client}</div>
            <div style="margin-top:36px;display:grid;grid-template-columns:1fr 1fr;gap:16px;padding-top:20px;border-top:1px solid var(--hairline)">
              <div>
                <div style="font-family:var(--font-mono);font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);margin-bottom:6px">Stack</div>
                <div style="font-size:13.5px">{c.stack}</div>
              </div>
              <div>
                <div style="font-family:var(--font-mono);font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:var(--muted);margin-bottom:6px">Impact</div>
                <div style="font-size:16px;color:var(--accent);font-weight:500">{ft(c.impact, lang)}</div>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* WRITING dark */}
  <section style="padding:clamp(120px,14vw,160px) clamp(20px,4vw,48px);background:var(--ink);color:var(--paper)">
    <div style="max-width:1600px;margin:0 auto;display:grid;grid-template-columns:400px 1fr;gap:clamp(40px,6vw,100px)">
      <div>
        <div style="font-family:var(--font-mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:rgba(246,241,232,.5)">
          {lang === 'fr' ? 'Écrits' : 'Writing'}
        </div>
        <div style="font-family:var(--font-display);font-size:clamp(36px,6vw,88px);line-height:.96;letter-spacing:-0.025em;margin-top:28px;color:var(--paper)">
          {lang === 'fr' ? (<>Notes de <i style="color:var(--accent)">terrain</i>, lentement.</>) : (<>Field <i style="color:var(--accent)">notes</i>, slowly.</>)}
        </div>
      </div>
      <div>
        {FIELD_NOTES.map((s) => (
          <a href={flh('/ressources/blog', lang)} class="b-write-row">
            <span style="font-family:var(--font-mono);font-size:11px;color:rgba(246,241,232,.5)">{s.date}</span>
            <span style="font-family:var(--font-mono);font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:rgba(246,241,232,.5)">{ft(s.tag, lang)}</span>
            <span style="font-family:var(--font-display);font-size:clamp(18px,1.8vw,26px);line-height:1.25;color:var(--paper)">{ft(s.text, lang)}</span>
            <span style="font-family:var(--font-mono);font-size:13px">↗</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* ACTE V · Stop rehearsing */}
  <section style="padding:clamp(160px,20vw,240px) clamp(20px,4vw,48px);background:var(--paper);text-align:center">
    <div style="max-width:1600px;margin:0 auto">
      <Kicker class="manifesto-acte-v">
        <span style="color:var(--accent)">V.</span> {lang === 'fr' ? "Passer à l'action" : 'Act'}
      </Kicker>
      <h2 class="reveal" style="font-family:var(--font-display);font-size:clamp(72px,14vw,260px);line-height:.86;letter-spacing:-0.04em;text-wrap:balance;font-weight:400">
        {lang === 'fr' ? (<>Arrêtez de <i style="color:var(--accent)">répéter</i>.<br />Livrez.</>) : (<>Stop <i style="color:var(--accent)">rehearsing</i>.<br />Ship.</>)}
      </h2>
      <div style="display:inline-flex;gap:14px;margin-top:60px;flex-wrap:wrap;justify-content:center">
        <Button href={flh('/contact', lang)} variant="accent" size="lg" mag={0.22}>
          {lang === 'fr' ? 'Prendre 45 min →' : 'Book 45 min →'}
        </Button>
        <Button href={`mailto:${lang === 'fr' ? 'bonjour@waimia.fr' : 'hello@waimia.fr'}`} variant="ghost" size="lg">
          {lang === 'fr' ? 'bonjour@waimia.fr' : 'hello@waimia.fr'}
        </Button>
      </div>
    </div>
  </section>
</Base>

<style>
  /* Margins pour les Kicker atom rendus par les sous-composants */
  :global(.manifesto-acte-iv){margin-bottom:32px}
  :global(.manifesto-acte-v){margin-bottom:40px}

  /* Marquee local · plus rapide que le marquee paper du / */
  .b-marquee-track{display:inline-flex;animation:b-slide 80s linear infinite}
  @keyframes b-slide{to{transform:translateX(-33.333%)}}
  @media (prefers-reduced-motion:reduce){.b-marquee-track{animation:none}}

  /* Service rows asymétriques */
  .b-service-row{display:grid;grid-template-columns:140px 1fr 2fr 100px;gap:clamp(20px,3vw,40px);align-items:baseline;padding:clamp(36px,4.5vw,48px) 0;border-top:1px solid var(--hairline)}
  .b-service-row[data-reverse="true"]{grid-template-columns:100px 2fr 1fr 140px}
  .b-svc-num{font-family:var(--font-display);font-size:clamp(40px,5vw,64px);line-height:.9;color:var(--accent);font-style:italic}
  .b-svc-name{font-family:var(--font-display);font-size:clamp(32px,4.5vw,56px);line-height:1;letter-spacing:-0.02em}
  .b-svc-body{font-size:clamp(13px,1.3vw,16px);line-height:1.55;color:var(--muted);max-width:500px}
  .b-svc-arr{font-family:var(--font-mono);font-size:13px;text-align:right;color:var(--accent)}
  .b-svc-arr-l{font-family:var(--font-mono);font-size:13px;color:var(--accent)}
  .b-svc-body-r{font-size:clamp(13px,1.3vw,16px);line-height:1.55;color:var(--muted);max-width:500px;margin-left:auto;text-align:right}
  .b-svc-name-r{font-family:var(--font-display);font-size:clamp(32px,4.5vw,56px);line-height:1;letter-spacing:-0.02em;text-align:right}
  .b-svc-num-r{font-family:var(--font-display);font-size:clamp(40px,5vw,64px);line-height:.9;color:var(--accent);font-style:italic;text-align:right}

  .b-case-card{display:block;padding:clamp(24px,3vw,40px);background:var(--paper-2);border-radius:18px;border:1px solid var(--hairline);transition:all 280ms var(--ease);color:inherit}
  .b-case-card:hover{border-color:var(--accent);transform:translateY(-4px)}

  .b-write-row{display:grid;grid-template-columns:90px 130px 1fr 20px;gap:24px;padding:22px 0;border-top:1px solid rgba(246,241,232,.12);align-items:baseline;color:inherit;transition:color 200ms var(--ease)}
  .b-write-row:hover{color:var(--accent)}

  @media (max-width:900px){
    .b-service-row,
    .b-service-row[data-reverse="true"]{grid-template-columns:1fr;gap:8px;padding:24px 0}
    .b-svc-arr,.b-svc-arr-l{display:none}
    .b-svc-body-r,.b-svc-name-r,.b-svc-num-r{text-align:left;margin-left:0}
    .b-write-row{grid-template-columns:1fr;gap:6px;padding:18px 0}
  }
</style>
