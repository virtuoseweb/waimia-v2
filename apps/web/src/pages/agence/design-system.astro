---
// Page static · /agence/design-system · CATALOGUE EXPOSABLE Phase C
//
// Source de vérité visible du Design System fermé Waimia (Tier 1 canonique + Tier 2 dérivés).
// Cf docs/18-DESIGN-SYSTEM-CARTOGRAPHY.md (audit) + docs/19-DESIGN-SYSTEM-CLOSED.md (système fermé)
//
// Mandat Simon (verbatim) : « créer un système fermer design système atomique pour éviter
// que sa parte dans tous les sens [...] l'agent IA pioche comme une librairie dans l'existant »
//
// Architecture :
//   0. Header + Sticky TOC
//   1. TOKENS (couleurs / typo / spacing / shadows / motion / z-index)
//   2. CLASSES UTILITY (layout / buttons / reveal / hairlines)
//   3. ATOMS (10)
//   4. MOLECULES (30 — sélection)
//   5. ORGANISMS (25 — Tier 2 dérivés autorisés)
//   6. SECTIONS W6 composable V2 (19 — Tier 1 cible)
//   7. EDITORIAL (8)
//   8. MOTION (4)
//   9. TEMPLATES (18 preview links)

export const prerender = true;

import Base from '../../layouts/Base.astro';
import Kicker from '../../components/ui/atoms/Kicker.astro';
import ChapterLabel from '../../components/ui/atoms/ChapterLabel.astro';
import Button from '../../components/ui/atoms/Button.astro';
import PillCTA from '../../components/ui/atoms/PillCTA.astro';
import TerminalCTA from '../../components/ui/atoms/TerminalCTA.astro';
import Bi from '../../components/ui/atoms/Bi.astro';
import SectionHeader from '../../components/ui/molecules/SectionHeader.astro';
import StatRow from '../../components/ui/molecules/StatRow.astro';
import TechPillRow from '../../components/ui/molecules/TechPillRow.astro';
import RelatedCards from '../../components/ui/molecules/RelatedCards.astro';

// Sections W6 composable V2 — showcase live §6
import HeroSplit from '../../components/sections/HeroSplit.astro';
import HeroCentered from '../../components/sections/HeroCentered.astro';
import HeroFullBleed from '../../components/sections/HeroFullBleed.astro';
import ProofBarSection from '../../components/sections/ProofBarSection.astro';
import StatBlock from '../../components/sections/StatBlock.astro';
import SocialProof from '../../components/sections/SocialProof.astro';
import MethodTimeline from '../../components/sections/MethodTimeline.astro';
import FeatureGrid from '../../components/sections/FeatureGrid.astro';
import ComparisonTable from '../../components/sections/ComparisonTable.astro';
import PricingTable from '../../components/sections/PricingTable.astro';
import FaqAccordion from '../../components/sections/FaqAccordion.astro';
import TestimonialBlock from '../../components/sections/TestimonialBlock.astro';
import GuaranteeBlock from '../../components/sections/GuaranteeBlock.astro';
import CtaInline from '../../components/sections/CtaInline.astro';
import CtaFinal from '../../components/sections/CtaFinal.astro';
---

<Base
  title="Design System · Catalogue Waimia · Tokens · Composants · Templates"
  description="Catalogue exposable du système design fermé Waimia. Source de vérité unique : pioche au lieu d'inventer."
  noindex
>
  {/* HEADER PAGE */}
  <section class="ds-hd">
    <div class="wrap-wide">
      <Kicker class="ds-kicker">§ Design System · Catalogue Waimia · v2026.05</Kicker>
      <h1 class="ds-h1">Pioche, n'<i>invente</i> pas.</h1>
      <p class="ds-lede">
        118 tokens canoniques. 64 classes utility Tier 1. 19 sections composables. 10 atomes.
        L'agent IA visite cette page en premier réflexe — recherche par section, copie le snippet,
        n'invente jamais un pattern qui existe déjà.
      </p>
      <p class="ds-meta">
        Cf <code>docs/18-DESIGN-SYSTEM-CARTOGRAPHY.md</code> (audit) ·
        <code>docs/19-DESIGN-SYSTEM-CLOSED.md</code> (système fermé)
      </p>
    </div>
  </section>

  {/* CONTENT + STICKY TOC */}
  <div class="ds-layout">
    <aside class="ds-toc">
      <div class="ds-toc-inner">
        <Kicker class="ds-toc-label">§ Sommaire</Kicker>
        <ol class="ds-toc-list">
          <li><a href="#tokens">1. Tokens</a></li>
          <li><a href="#classes">2. Classes utility</a></li>
          <li><a href="#atoms">3. Atoms</a></li>
          <li><a href="#molecules">4. Molecules</a></li>
          <li><a href="#organisms">5. Organisms (Tier 2)</a></li>
          <li><a href="#sections">6. Sections W6 (Tier 1)</a></li>
          <li><a href="#editorial">7. Editorial</a></li>
          <li><a href="#motion">8. Motion</a></li>
          <li><a href="#templates">9. Templates</a></li>
        </ol>
      </div>
    </aside>

    <main class="ds-content">
      {/* ───────────── 1. TOKENS ───────────── */}
      <section id="tokens" class="ds-section">
        <ChapterLabel num="01" title="Tokens" />
        <p class="ds-section-lede">
          118 tokens CSS canoniques exposés à <code>:root</code>. Source : <code>src/styles/tokens.css</code>.
          L'agent IA <strong>n'invente jamais</strong> de nouveau token sans procédure §B.5 doc 19.
        </p>

        {/* 1.1 Palette couleurs */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.1 · Palette couleurs (11 tokens)</h3>
          <div class="ds-swatch-grid">
            <div class="ds-swatch-card">
              <span class="ds-swatch ds-swatch--paper"></span>
              <div class="ds-swatch-info">
                <code>--paper</code>
                <span class="ds-swatch-hex">#F6F1E8</span>
                <span class="ds-swatch-role">background canonique</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--paper-2)"></span>
              <div class="ds-swatch-info">
                <code>--paper-2</code>
                <span class="ds-swatch-hex">#EFE9DD</span>
                <span class="ds-swatch-role">bg subtle</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--paper-3)"></span>
              <div class="ds-swatch-info">
                <code>--paper-3</code>
                <span class="ds-swatch-hex">#E6E0D0</span>
                <span class="ds-swatch-role">bg raised</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--ink);border-color:var(--hairline-strong)"></span>
              <div class="ds-swatch-info">
                <code>--ink</code>
                <span class="ds-swatch-hex">#0C0B09</span>
                <span class="ds-swatch-role">texte primaire</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--ink-2)"></span>
              <div class="ds-swatch-info">
                <code>--ink-2</code>
                <span class="ds-swatch-hex">#1A1814</span>
                <span class="ds-swatch-role">texte strong</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--ink-3)"></span>
              <div class="ds-swatch-info">
                <code>--ink-3</code>
                <span class="ds-swatch-hex">#2B2620</span>
                <span class="ds-swatch-role">texte tertiaire dark</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--muted)"></span>
              <div class="ds-swatch-info">
                <code>--muted</code>
                <span class="ds-swatch-hex">#6B6560</span>
                <span class="ds-swatch-role">texte secondaire</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--muted-2)"></span>
              <div class="ds-swatch-info">
                <code>--muted-2</code>
                <span class="ds-swatch-hex">#9A948D</span>
                <span class="ds-swatch-role">texte tertiaire</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--accent)"></span>
              <div class="ds-swatch-info">
                <code>--accent</code>
                <span class="ds-swatch-hex">#C94F2E</span>
                <span class="ds-swatch-role">brand primary · terracotta</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--accent-2)"></span>
              <div class="ds-swatch-info">
                <code>--accent-2</code>
                <span class="ds-swatch-hex">#A83D20</span>
                <span class="ds-swatch-role">accent hover</span>
              </div>
            </div>
            <div class="ds-swatch-card">
              <span class="ds-swatch" style="background:var(--accent-ink)"></span>
              <div class="ds-swatch-info">
                <code>--accent-ink</code>
                <span class="ds-swatch-hex">#F6F1E8</span>
                <span class="ds-swatch-role">texte sur accent</span>
              </div>
            </div>
          </div>
        </div>

        {/* 1.2 Typographie */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.2 · Typographie</h3>
          <p class="ds-help">3 stacks canoniques + échelle V6 éditoriale + scale enrichie.</p>

          <div class="ds-type-stacks">
            <div class="ds-type-stack">
              <Kicker tone="muted" size={10}>--font-display · Instrument Serif</Kicker>
              <p style="font-family:var(--font-display);font-size:48px;line-height:1.02;margin:8px 0 0">
                Pioche, n'<i>invente</i> pas.
              </p>
            </div>
            <div class="ds-type-stack">
              <Kicker tone="muted" size={10}>--font-sans · Inter Tight</Kicker>
              <p style="font-family:var(--font-sans);font-size:17px;line-height:1.5;margin:8px 0 0">
                Le système design est une librairie, pas un terrain d'invention.
                Chaque atome existe pour une raison documentée.
              </p>
            </div>
            <div class="ds-type-stack">
              <Kicker tone="muted" size={10}>--font-mono · JetBrains Mono</Kicker>
              <p style="font-family:var(--font-mono);font-size:13px;letter-spacing:.06em;margin:8px 0 0">
                <code>grep -oE "var\(--[a-z0-9-]+\)" src/components/</code>
              </p>
            </div>
          </div>

          <h4 class="ds-h4">Échelle V6 éditoriale (7 niveaux)</h4>
          <div class="ds-type-scale">
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h1-hero</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h1-hero);line-height:.96;margin:6px 0 0">H1 hero · manifeste de page</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h2-grand</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h2-grand);line-height:1.02;margin:6px 0 0">H2 grand · climax final</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h2-section</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h2-section);line-height:1.1;margin:6px 0 0">H2 section · titre par défaut</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h2-tight</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h2-tight);line-height:1.1;margin:6px 0 0">H2 tight · FAQ, sidebar</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h3-row</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h3-row);line-height:1.1;margin:6px 0 0">H3 row · Departments, Notes</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h3-card</Kicker>
              <p style="font-family:var(--font-display);font-size:var(--text-h3-card);line-height:1.1;margin:6px 0 0">H3 card · CapabilityStrip, SixServices</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-h4-meta</Kicker>
              <p style="font-family:var(--font-sans);font-size:var(--text-h4-meta);font-weight:600;margin:6px 0 0">H4 meta · sub-titles, labels inline</p>
            </div>
          </div>

          <h4 class="ds-h4">Échelle enrichie (eyebrow / lede / pull-quote / drop-cap / caption)</h4>
          <div class="ds-type-scale">
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-eyebrow</Kicker>
              <p style="font-family:var(--font-mono);font-size:var(--text-eyebrow);letter-spacing:.14em;text-transform:uppercase;color:var(--muted);margin:6px 0 0">Eyebrow · section markers</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-lede</Kicker>
              <p style="font-family:var(--font-sans);font-size:var(--text-lede);color:var(--ink-2);line-height:1.45;margin:6px 0 0;max-width:60ch">Lede · paragraphe d'ouverture surdimensionné qui pose la promesse de la section.</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-pull-quote</Kicker>
              <p style="font-family:var(--font-display);font-style:italic;font-size:var(--text-pull-quote);color:var(--ink);line-height:1.15;margin:6px 0 0;max-width:48ch">« Pull quote · citation breakout »</p>
            </div>
            <div class="ds-type-row">
              <Kicker tone="muted" size={10}>--text-caption</Kicker>
              <p style="font-family:var(--font-sans);font-size:var(--text-caption);font-style:italic;color:var(--muted);margin:6px 0 0">Caption · légende fine pour figures et tables</p>
            </div>
          </div>
        </div>

        {/* 1.3 Spacing scale */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.3 · Spacing scale (8pt · 17 tokens)</h3>
          <div class="ds-spacing-grid">
            <div class="ds-spacing-row"><code>--space-1</code> <span class="ds-spacing-bar" style="width:4px"></span> <span class="ds-spacing-val">4px</span></div>
            <div class="ds-spacing-row"><code>--space-2</code> <span class="ds-spacing-bar" style="width:8px"></span> <span class="ds-spacing-val">8px</span></div>
            <div class="ds-spacing-row"><code>--space-3</code> <span class="ds-spacing-bar" style="width:12px"></span> <span class="ds-spacing-val">12px</span></div>
            <div class="ds-spacing-row"><code>--space-4</code> <span class="ds-spacing-bar" style="width:16px"></span> <span class="ds-spacing-val">16px</span></div>
            <div class="ds-spacing-row"><code>--space-5</code> <span class="ds-spacing-bar" style="width:24px"></span> <span class="ds-spacing-val">24px</span></div>
            <div class="ds-spacing-row"><code>--space-6</code> <span class="ds-spacing-bar" style="width:32px"></span> <span class="ds-spacing-val">32px</span></div>
            <div class="ds-spacing-row"><code>--space-7</code> <span class="ds-spacing-bar" style="width:40px"></span> <span class="ds-spacing-val">40px</span></div>
            <div class="ds-spacing-row"><code>--space-8</code> <span class="ds-spacing-bar" style="width:48px"></span> <span class="ds-spacing-val">48px</span></div>
            <div class="ds-spacing-row"><code>--space-9</code> <span class="ds-spacing-bar" style="width:64px"></span> <span class="ds-spacing-val">64px</span></div>
            <div class="ds-spacing-row"><code>--space-10</code> <span class="ds-spacing-bar" style="width:80px"></span> <span class="ds-spacing-val">80px</span></div>
            <div class="ds-spacing-row"><code>--space-11</code> <span class="ds-spacing-bar" style="width:96px"></span> <span class="ds-spacing-val">96px</span></div>
            <div class="ds-spacing-row"><code>--space-12</code> <span class="ds-spacing-bar" style="width:120px"></span> <span class="ds-spacing-val">120px</span></div>
          </div>
          <p class="ds-help">+ section rhythm : <code>--section-tight</code> 48px · <code>--section-base</code> 96px · <code>--section-loose</code> 160px · <code>--section-grand</code> 256px</p>
        </div>

        {/* 1.4 Shadows */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.4 · Shadows éditoriaux (5 tokens)</h3>
          <div class="ds-shadow-grid">
            <div class="ds-shadow-card" style="box-shadow:var(--shadow-hairline)">
              <code>--shadow-hairline</code>
              <span class="ds-help">simple 1px hairline</span>
            </div>
            <div class="ds-shadow-card" style="box-shadow:var(--shadow-paper-lift)">
              <code>--shadow-paper-lift</code>
              <span class="ds-help">lift léger éditorial</span>
            </div>
            <div class="ds-shadow-card" style="box-shadow:var(--shadow-card-rest)">
              <code>--shadow-card-rest</code>
              <span class="ds-help">card au repos</span>
            </div>
            <div class="ds-shadow-card" style="box-shadow:var(--shadow-card-hover)">
              <code>--shadow-card-hover</code>
              <span class="ds-help">card hover state</span>
            </div>
            <div class="ds-shadow-card" style="box-shadow:var(--shadow-cta)">
              <code>--shadow-cta</code>
              <span class="ds-help">CTA principal · accent</span>
            </div>
          </div>
        </div>

        {/* 1.5 Easings + durations */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.5 · Motion (3 easings + 7 durations)</h3>
          <div class="ds-motion-grid">
            <div class="ds-motion-card">
              <code>--ease</code>
              <span class="ds-help">cubic-bezier(0.22, 1, 0.36, 1) · défaut</span>
            </div>
            <div class="ds-motion-card">
              <code>--ease-out</code>
              <span class="ds-help">cubic-bezier(0.16, 1, 0.30, 1)</span>
            </div>
            <div class="ds-motion-card">
              <code>--ease-sharp</code>
              <span class="ds-help">cubic-bezier(0.20, 0, 0.00, 1)</span>
            </div>
          </div>
          <ul class="ds-durations">
            <li><code>--dur-instant</code> 0ms</li>
            <li><code>--dur-snap</code> 120ms</li>
            <li><code>--dur-fast</code> 200ms</li>
            <li><code>--dur-base</code> 400ms</li>
            <li><code>--dur-slow</code> 800ms</li>
            <li><code>--dur-deliberate</code> 1200ms</li>
            <li><code>--dur-cinematic</code> 1800ms</li>
          </ul>
        </div>

        {/* 1.6 Z-index */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.6 · Z-index scale (6 niveaux)</h3>
          <ul class="ds-z-list">
            <li><code>--z-base</code> 1</li>
            <li><code>--z-sticky</code> 10</li>
            <li><code>--z-overlay</code> 100</li>
            <li><code>--z-modal</code> 1000</li>
            <li><code>--z-toast</code> 2000</li>
            <li><code>--z-cursor</code> 9999</li>
          </ul>
        </div>

        {/* 1.7 Layout */}
        <div class="ds-subsection">
          <h3 class="ds-h3">1.7 · Layout & containers</h3>
          <ul class="ds-z-list">
            <li><code>--wrap</code> 1440px · max-width standard</li>
            <li><code>--wrap-wide</code> 1560px · max-width étendu (sections)</li>
            <li><code>--wrap-tight</code> 1120px · prose article</li>
            <li><code>--gut</code> 32px (responsive : 20px ≤ 900px · 16px ≤ 600px)</li>
            <li><code>--header-height</code> 72px · clearance sticky obligatoire pour heros</li>
          </ul>
        </div>
      </section>

      {/* ───────────── 2. CLASSES UTILITY ───────────── */}
      <section id="classes" class="ds-section">
        <ChapterLabel num="02" title="Classes utility" />
        <p class="ds-section-lede">
          64 classes Tier 1 figées dans <code>src/styles/global.css</code>. Préférer <code>Button.astro</code>
          atom plutôt que <code>.btn</code> class pour les nouveaux composants. Migration progressive en cours.
        </p>

        <div class="ds-subsection">
          <h3 class="ds-h3">2.1 · Layout containers (3 classes)</h3>
          <div class="ds-class-card">
            <code class="ds-class-name">.wrap</code>
            <span class="ds-help">max-width: <code>var(--wrap)</code> · padding-inline: <code>var(--gut)</code> · margin auto</span>
          </div>
          <div class="ds-class-card">
            <code class="ds-class-name">.wrap-wide</code>
            <span class="ds-help">max-width: <code>var(--wrap-wide)</code> · obligatoire pour sections W6</span>
          </div>
          <div class="ds-class-card">
            <code class="ds-class-name">.wrap-tight</code>
            <span class="ds-help">max-width: <code>var(--wrap-tight)</code> · prose article</span>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">2.2 · Buttons (9 classes — préférer <code>Button.astro</code> atom)</h3>
          <p class="ds-help">Les classes <code>.btn-*</code> survivent pour legacy. Pour nouveau code : <code>&lt;Button variant="primary" /&gt;</code>.</p>
          <div class="ds-row">
            <button class="btn btn-primary">.btn-primary</button>
            <button class="btn btn-accent">.btn-accent</button>
            <button class="btn btn-ghost">.btn-ghost</button>
            <button class="btn btn-primary btn-lg">.btn-lg</button>
            <a href="#" class="btn-link">.btn-link →</a>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">2.3 · Reveal animations (5 patterns)</h3>
          <ul class="ds-z-list">
            <li><code>.blur-in</code> · entrance avec flou progressif</li>
            <li><code>.clip-up</code> · clip-path révèle bas→haut</li>
            <li><code>.reveal-fade</code> · fade simple</li>
            <li><code>.reveal-up</code> · translate-y + fade</li>
            <li><code>.scale-in</code> · scale 0.95 → 1 + fade</li>
          </ul>
          <p class="ds-help">Toutes les classes ajoutent state <code>.in</code> via IntersectionObserver (cf <code>src/lib/animations/scroll-reveal.ts</code>).</p>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">2.4 · Hairlines (3 classes)</h3>
          <div class="ds-hairlines">
            <div><code>.hline</code></div>
            <hr class="hline" />
            <div><code>.hline-strong</code></div>
            <hr class="hline-strong" />
          </div>
        </div>
      </section>

      {/* ───────────── 3. ATOMS ───────────── */}
      <section id="atoms" class="ds-section">
        <ChapterLabel num="03" title="Atoms (10)" />
        <p class="ds-section-lede">
          Briques minimales · Tier 1 canonique. Préférer ces composants Astro plutôt que des classes CSS
          équivalentes. Source : <code>src/components/ui/atoms/</code>.
        </p>

        <div class="ds-subsection">
          <h3 class="ds-h3">Kicker · 3 tones</h3>
          <div class="ds-row">
            <Kicker>§ TONE MUTED</Kicker>
            <Kicker tone="accent">§ TONE ACCENT</Kicker>
            <Kicker tone="dim">§ TONE DIM (ON DARK)</Kicker>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">ChapterLabel</h3>
          <ChapterLabel num="II · 2.0" title="Acte II — La méthode" />
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">Button · 4 variants</h3>
          <div class="ds-row" style="gap:12px">
            <Button href="#" variant="primary">Primary</Button>
            <Button href="#" variant="ghost">Ghost</Button>
            <Button href="#" variant="accent">Accent</Button>
            <Button href="#" variant="link">Link →</Button>
          </div>
          <div class="ds-row" style="gap:12px;margin-top:14px">
            <Button href="#" variant="primary" size="lg" mag={0.18}>Primary · large · magnetic</Button>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">PillCTA</h3>
          <div class="ds-row" style="gap:12px">
            <PillCTA href="#" variant="accent">Pill accent</PillCTA>
            <PillCTA href="#" variant="ghost-dark">Pill ghost-dark</PillCTA>
            <PillCTA href="#" variant="accent-mono">▸ Pill accent-mono</PillCTA>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">TerminalCTA</h3>
          <div class="ds-row" style="gap:12px">
            <TerminalCTA href="#" variant="ink">▸ ouvrir la console</TerminalCTA>
            <TerminalCTA href="#" variant="ink-ghost">prendre une séance →</TerminalCTA>
            <TerminalCTA href="#" variant="accent">▸ schedule</TerminalCTA>
          </div>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">Bi · bilingue (FR/EN)</h3>
          <p>Mode props : <Bi fr="Bonjour" en="Hello" /></p>
          <p>Mode slots :
            <Bi>
              <Fragment slot="fr">Construire du <i style="color:var(--accent)">revenue</i>.</Fragment>
              <Fragment slot="en">Build <i style="color:var(--accent)">revenue</i>.</Fragment>
            </Bi>
          </p>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">Atomes non-démo (4)</h3>
          <ul class="ds-z-list">
            <li><code>CursorDot</code> · cursor custom injecté par Base.astro</li>
            <li><code>ProgressBar</code> · scroll progress bar top</li>
            <li><code>ScrollProgress</code> · progression scroll étendue</li>
            <li><code>SectionTransition</code> · phrase éditoriale entre sections (cf home)</li>
          </ul>
        </div>
      </section>

      {/* ───────────── 4. MOLECULES (sélection) ───────────── */}
      <section id="molecules" class="ds-section">
        <ChapterLabel num="04" title="Molecules (30 — sélection)" />
        <p class="ds-section-lede">
          Compositions fonctionnelles · Tier 1. Cf <code>src/components/ui/molecules/</code> pour les 30 fichiers.
          Sélection ci-dessous : les plus utilisés (≥ 3 imports).
        </p>

        <div class="ds-subsection">
          <h3 class="ds-h3">SectionHeader (legacy .sec-hd)</h3>
          <SectionHeader roman="II" label="Acte II — Démo" id="ds-demo-header">
            <Fragment slot="heading">Quatre <i>paliers</i>, une seule architecture.</Fragment>
            <Fragment slot="aside">Du diagnostic à l'infrastructure souveraine.</Fragment>
          </SectionHeader>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">StatRow</h3>
          <StatRow stats={[
            { k: '43',    label_fr: 'interventions',   label_en: 'engagements' },
            { k: '€18M+', label_fr: 'pipeline audité', label_en: 'pipeline audited' },
            { k: '× 4',   label_fr: 'taux de réussite', label_en: 'success rate' },
            { k: '12',    label_fr: 'juridictions UE',  label_en: 'EU jurisdictions' },
          ]} />
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">TechPillRow</h3>
          <TechPillRow pills={[
            { label: 'Claude Opus', kind: 'primary' },
            { label: 'PostgreSQL',  kind: 'open' },
            { label: 'HubSpot',     kind: 'neutral' },
            { label: 'dbt',         kind: 'open' },
          ]} />
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">RelatedCards (3 cards · pattern fin de page)</h3>
          <RelatedCards cards={[
            { kicker: 'CASE 04', title_fr: 'Plateau SaaS', title_en: 'Plateau SaaS', lead_fr: '+€2.4M pipeline →', lead_en: '+€2.4M pipeline →', href: '/cas/plateau' },
            { kicker: 'OFFRE 03.1', title_fr: 'RevOps Rebuild', title_en: 'RevOps Rebuild', lead_fr: 'Le service inspiré →', lead_en: 'Inspired service →', href: '/offres/revops' },
            { kicker: 'TRUST', title_fr: 'Trust Center', title_en: 'Trust Center', lead_fr: 'Politique données →', lead_en: 'Data policy →', href: '/agence/trust-center' },
          ]} />
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">Molecules non-démo (26)</h3>
          <p class="ds-help">Voir <code>src/components/ui/molecules/</code> pour : AsymmetricServiceRow, AuthorByline, AuthorCard, Breadcrumb, CalEmbed, EditorialCaseCard, EditorialWriteRow, FitColumns, MastheadRow, MetricStrip, NewsletterSignup, PagePill, PricingTier, ProcessSteps, ProofBand, RelatedByCluster, ServiceCatalogRow, ShareButtons, TableOfContents, TagPills, TaxonomyMenu, TerminalMockup, TerminalTable, TunnelNav.</p>
        </div>
      </section>

      {/* ───────────── 5. ORGANISMS (Tier 2 dérivés) ───────────── */}
      <section id="organisms" class="ds-section">
        <ChapterLabel num="05" title="Organisms (25 — Tier 2 dérivés)" />
        <p class="ds-section-lede">
          Organismes historiques pour pages standalone (home FR/EN principalement). Tier 2 dérivés
          autorisés pour casser monotonie. Cf <code>src/components/ui/organisms/</code>.
        </p>
        <p class="ds-help">
          ⚠️ <strong>Pour pages composables</strong> : préférer les sections W6 §6 ci-dessous (Tier 1 cible).
          Organismes pour cas spécifiques uniquement (home Hero, BookSession climax, Footer*).
        </p>
        <p class="ds-help">
          Liste : AtlasGrid, BookSession, CapabilityStrip, Cases, CookiesBanner, CtaBand,
          Departments, FieldNotes, FooterMarquee, Hero, HowWeShip, Manifesto, ManifestoAccent,
          Offices, OperatingLayer, PersonaSwitcher, ProofBar, Pyramid, SigBand, SixServices,
          Stub, SystemArchitecture, TrustMarquee, WhyAIStalls, WritingNotes.
        </p>
        <p class="ds-help">
          Preview live : <a href="/" class="ds-link">/ (home FR)</a> · <a href="/manifesto" class="ds-link">/manifesto</a> · <a href="/atlas" class="ds-link">/atlas</a>
        </p>
      </section>

      {/* ───────────── 6. SECTIONS W6 composable V2 (Tier 1 cible) ───────────── */}
      <section id="sections" class="ds-section">
        <ChapterLabel num="06" title="Sections W6 composable V2 (19 — Tier 1 cible)" />
        <p class="ds-section-lede">
          Architecture composable Tier 1 cible. Toutes les nouvelles pages doivent utiliser ces sections
          via <code>SectionsRenderer</code>. Cf <code>src/components/sections/</code>.
          <strong>Toutes containerisées</strong> avec <code>.wrap-wide</code> sauf exceptions edge-to-edge documentées.
        </p>

        <div class="ds-subsection">
          <h3 class="ds-h3">Pattern d'usage</h3>
          <pre class="ds-code"><code>{`<SectionsRenderer
  sections={[
    { type: 'hero-split', h1_fr: '...', cta_primary: {...} },
    { type: 'proof-bar', metrics: [...] },
    { type: 'method-timeline', steps: [...] },
    { type: 'cta-final', h2_fr: '...' },
  ]}
  lang="fr"
/>`}</code></pre>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">19 sections disponibles</h3>
          <ul class="ds-sections-list">
            <li><strong>HeroSplit</strong> · hero avec right-block (mini-map, workflow, stats)</li>
            <li><strong>HeroCentered</strong> · hero centré + CTA stack</li>
            <li><strong>HeroFullBleed</strong> · hero full-width edge-to-edge</li>
            <li><strong>ProofBarSection</strong> · 4-6 métriques en grille</li>
            <li><strong>StatBlock</strong> · stats riches avec h2 + lede</li>
            <li><strong>SocialProof</strong> · logos marquee ou grid (full-bleed)</li>
            <li><strong>MethodTimeline</strong> · timeline horizontale numérotée</li>
            <li><strong>FeatureGrid</strong> · 3-8 features en grid (2/3/4 cols)</li>
            <li><strong>ComparisonTable</strong> · comparatif features × tiers</li>
            <li><strong>TimelineBlock</strong> · timeline narrative avec dates</li>
            <li><strong>MediaBlock</strong> · image/video/svg/embed avec caption</li>
            <li><strong>PricingTable</strong> · 1-5 tiers tarifaires</li>
            <li><strong>FaqAccordion</strong> · FAQ avec details/summary</li>
            <li><strong>ObjectionHandler</strong> · objection/réponse en table</li>
            <li><strong>CtaFinal</strong> · CTA fin de page (bg ink/accent/paper)</li>
            <li><strong>CtaBandSection</strong> · band horizontal full-bleed</li>
            <li><strong>CtaInline</strong> · CTA discret en milieu de page</li>
            <li><strong>TestimonialBlock</strong> · témoignages (carousel/grid/rows)</li>
            <li><strong>GuaranteeBlock</strong> · garanties numérotées</li>
          </ul>
          <p class="ds-help">
            Preview live : <a href="/test-composable" class="ds-link">/test-composable</a>
            (5 sections inline pour démo end-to-end)
          </p>
        </div>

        <div class="ds-subsection">
          <h3 class="ds-h3">Showcase live · 6 sections les plus utilisées</h3>
          <p class="ds-help">Démos avec mock data. Pattern d'usage in-page.</p>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>HeroSplit · type "hero-split" (right_block: mockup)</Kicker>
            <div class="ds-section-demo-frame ds-demo-hero">
              <HeroSplit data={{
                type: 'hero-split',
                kicker_fr: 'Démo Hero · Split layout',
                kicker_en: 'Demo Hero · Split layout',
                h1_fr: 'L\'agent IA <i>pioche</i>, ne réinvente pas.',
                h1_en: 'The AI agent <i>picks</i>, never reinvents.',
                lede_fr: 'Le composable W6 expose 19 sections cataloguées. Chaque section atomique, testée, validée.',
                lede_en: 'W6 composable exposes 19 catalogued sections. Each atomic, tested, validated.',
                cta_primary: { label_fr: 'Voir le catalogue →', label_en: 'View catalogue →', href: '/agence/design-system', variant: 'primary' },
                cta_secondary: { label_fr: 'Lire la doc', label_en: 'Read the docs', href: '/agence/docs', variant: 'ghost' },
                right_block: { kind: 'workflow-svg' },
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>HeroCentered · type "hero-centered"</Kicker>
            <div class="ds-section-demo-frame ds-demo-hero">
              <HeroCentered data={{
                type: 'hero-centered',
                kicker_fr: 'Démo · Centred layout',
                kicker_en: 'Demo · Centred layout',
                h1_fr: 'Hero <i>centered</i> pour pages standalone',
                h1_en: 'Hero <i>centered</i> for standalone pages',
                lede_fr: 'Variante minimaliste pour manifestes ou pages d\'introduction sans right-block.',
                lede_en: 'Minimal variant for manifestos or intro pages without right-block.',
                cta_primary: { label_fr: 'Continuer →', label_en: 'Continue →', href: '#', variant: 'primary' },
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>ProofBarSection · type "proof-bar" (layout: horizontal)</Kicker>
            <div class="ds-section-demo-frame">
              <ProofBarSection data={{
                type: 'proof-bar',
                kicker_fr: 'Preuves · démo design system',
                kicker_en: 'Proof · design system demo',
                metrics: [
                  { value: '118', label_fr: 'tokens canoniques', label_en: 'canonical tokens', emphasis: true },
                  { value: '19', label_fr: 'sections W6', label_en: 'W6 sections', emphasis: false },
                  { value: '114', label_fr: 'composants atomic', label_en: 'atomic components', emphasis: false },
                  { value: '0', label_fr: 'hallucinations', label_en: 'hallucinations', emphasis: false },
                ],
                layout: 'horizontal',
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>FeatureGrid · type "feature-grid" (columns: 3)</Kicker>
            <div class="ds-section-demo-frame">
              <FeatureGrid data={{
                type: 'feature-grid',
                kicker_fr: 'Démo · grille features',
                kicker_en: 'Demo · features grid',
                h2_fr: 'Trois <i>piliers</i> du DS Waimia',
                h2_en: 'Three <i>pillars</i> of Waimia DS',
                features: [
                  { title_fr: 'Tokens canoniques', title_en: 'Canonical tokens', body_fr: '118 tokens CSS exposés à :root. Pioche, n\'invente pas.', body_en: '118 CSS tokens at :root. Pick, never invent.' },
                  { title_fr: 'Composants atomiques', title_en: 'Atomic components', body_fr: '114 composants en 7 couches Tier 1.', body_en: '114 components across 7 Tier 1 layers.' },
                  { title_fr: 'Anti-hallucination', title_en: 'Anti-hallucination', body_fr: '5 anti-patterns documentés avec commandes de détection.', body_en: '5 anti-patterns documented with detection commands.' },
                ],
                columns: '3',
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>MethodTimeline · type "method-timeline"</Kicker>
            <div class="ds-section-demo-frame">
              <MethodTimeline data={{
                type: 'method-timeline',
                kicker_fr: 'Méthode · 4 étapes',
                kicker_en: 'Method · 4 steps',
                h2_fr: 'Le <i>processus</i> en quatre temps',
                h2_en: 'The <i>process</i> in four steps',
                lede_fr: 'De l\'audit initial au déploiement, chaque étape produit un livrable mesurable.',
                lede_en: 'From initial audit to deployment, each step ships a measurable artifact.',
                steps: [
                  { num: '01', label_fr: 'Audit', label_en: 'Audit', body_fr: 'Cartographie stack, données, équipe, frictions.', body_en: 'Map stack, data, team, frictions.' },
                  { num: '02', label_fr: 'Diagnostic', label_en: 'Diagnostic', body_fr: 'Priorisation des chantiers par ROI mesurable.', body_en: 'Prioritization by measurable ROI.' },
                  { num: '03', label_fr: 'Pilote', label_en: 'Pilot', body_fr: 'Premier workflow en production sous 5 jours.', body_en: 'First workflow in production in 5 days.' },
                  { num: '04', label_fr: 'Industrialisation', label_en: 'Industrialization', body_fr: 'Extension à 6-10 semaines après mesure.', body_en: 'Extension over 6-10 weeks after measurement.' },
                ],
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>FaqAccordion · type "faq-accordion"</Kicker>
            <div class="ds-section-demo-frame">
              <FaqAccordion data={{
                type: 'faq-accordion',
                kicker_fr: 'FAQ · 3 questions',
                kicker_en: 'FAQ · 3 questions',
                h2_fr: 'Questions <i>fréquentes</i>',
                h2_en: '<i>Frequent</i> questions',
                items: [
                  { q_fr: 'Combien de temps prend un projet ?', q_en: 'How long does a project take?', a_fr: 'AI Activation · 1 semaine. Growth System · 6 à 10 semaines.', a_en: 'AI Activation · 1 week. Growth System · 6 to 10 weeks.' },
                  { q_fr: 'Quels sont vos tarifs ?', q_en: 'What are your rates?', a_fr: 'AI Activation à partir de 1 500€. Growth System à partir de 5 000€.', a_en: 'AI Activation starts at €1,500. Growth System starts at €5,000.' },
                  { q_fr: 'Compatible avec notre stack actuelle ?', q_en: 'Compatible with our current stack?', a_fr: 'Oui — HubSpot, Salesforce, Notion, Excel, GA4, etc.', a_en: 'Yes — HubSpot, Salesforce, Notion, Excel, GA4, etc.' },
                ],
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>HeroFullBleed · type "hero-full-bleed" (bg signature-grid)</Kicker>
            <div class="ds-section-demo-frame ds-demo-hero">
              <HeroFullBleed data={{
                type: 'hero-full-bleed',
                h1_fr: 'Hero <i>full-bleed</i>',
                h1_en: 'Full-bleed <i>hero</i>',
                lede_fr: 'Variant signature pour pages d\'impact maximal.',
                lede_en: 'Signature variant for max-impact pages.',
                background_kind: 'signature-grid',
                cta_primary: { label_fr: 'Action →', label_en: 'Action →', href: '#', variant: 'primary' },
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>SocialProof · type "social-proof" (marquee)</Kicker>
            <div class="ds-section-demo-frame">
              <SocialProof data={{
                type: 'social-proof',
                kicker_fr: 'Ils ont câblé l\'IA avec Waimia',
                kicker_en: 'They wired AI with Waimia',
                logos: [
                  { name: 'PME Services' }, { name: 'ETI Industrie' }, { name: 'Cabinet Conseil' },
                  { name: 'Startup B2B' }, { name: 'Agence Marketing' }, { name: 'Cabinet Compta' },
                ],
                marquee: true,
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>ComparisonTable · type "comparison-table"</Kicker>
            <div class="ds-section-demo-frame">
              <ComparisonTable data={{
                type: 'comparison-table',
                h2_fr: 'Comparatif <i>tiers</i>',
                h2_en: 'Tiers <i>comparison</i>',
                column_headers_fr: ['Capacité', 'Activation 1 sem.', 'Growth System', 'Sur-mesure'],
                column_headers_en: ['Capability', 'Activation 1wk', 'Growth System', 'Custom'],
                rows: [
                  { feature_fr: 'Audit complet', feature_en: 'Full audit', values: [true, true, true] },
                  { feature_fr: 'Workflows IA', feature_en: 'AI workflows', values: ['1', '5-8', 'illimité'] },
                  { feature_fr: 'Formation équipe', feature_en: 'Team training', values: [false, true, true] },
                  { feature_fr: 'Support 90j', feature_en: '90d support', values: [false, true, true] },
                ],
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>PricingTable · type "pricing-table" (3 tiers)</Kicker>
            <div class="ds-section-demo-frame">
              <PricingTable data={{
                type: 'pricing-table',
                kicker_fr: 'Tarifs · démo',
                kicker_en: 'Pricing · demo',
                h2_fr: 'Trois <i>paliers</i>',
                h2_en: 'Three <i>tiers</i>',
                tiers: [
                  { name_fr: 'Activation', name_en: 'Activation', price: '€1,500', features_fr: ['1 workflow', 'Audit 5 jours', 'Formation 1 jour'], features_en: ['1 workflow', '5-day audit', '1-day training'], cta: { label_fr: 'Démarrer', label_en: 'Start', href: '#', variant: 'ghost' }, featured: false },
                  { name_fr: 'Growth', name_en: 'Growth', price: '€5,000', features_fr: ['5-8 workflows', 'Système complet 6-10 sem.', 'Support 90j'], features_en: ['5-8 workflows', 'Full system 6-10wk', '90d support'], cta: { label_fr: 'Choisir', label_en: 'Choose', href: '#', variant: 'primary' }, featured: true },
                  { name_fr: 'Sur-mesure', name_en: 'Custom', price: 'Devis', features_fr: ['Workflows illimités', 'Architecture dédiée', 'Support 12 mois'], features_en: ['Unlimited workflows', 'Custom architecture', '12mo support'], cta: { label_fr: 'Discuter', label_en: 'Talk', href: '#', variant: 'ghost' }, featured: false },
                ],
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>StatBlock · type "stat-block" (layout: grid-3)</Kicker>
            <div class="ds-section-demo-frame">
              <StatBlock data={{
                type: 'stat-block',
                h2_fr: 'Mesures <i>vérifiables</i>',
                h2_en: '<i>Verifiable</i> measurements',
                lede_fr: 'Statistiques observées sur 12 mois d\'engagements PME/ETI.',
                lede_en: 'Stats observed over 12 months of SMB engagements.',
                metrics: [
                  { value: '43', label_fr: 'engagements', label_en: 'engagements', emphasis: true },
                  { value: '€18M+', label_fr: 'pipeline audité', label_en: 'pipeline audited', emphasis: false },
                  { value: '× 3.7', label_fr: 'ROI moyen 12 mois', label_en: 'avg ROI 12 months', emphasis: false },
                ],
                layout: 'grid-3',
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>TestimonialBlock · type "testimonial-block" (layout: grid-2)</Kicker>
            <div class="ds-section-demo-frame">
              <TestimonialBlock data={{
                type: 'testimonial-block',
                kicker_fr: 'Témoignages · démo',
                kicker_en: 'Testimonials · demo',
                h2_fr: 'Ce que disent les <i>clients</i>',
                h2_en: 'What <i>clients</i> say',
                testimonials: [
                  { quote_fr: 'En 5 jours, on a câblé l\'IA dans nos workflows. Premier ROI à 6 semaines.', quote_en: 'In 5 days we wired AI into our workflows. First ROI at 6 weeks.', author: 'Marie L.', role_fr: 'COO', role_en: 'COO', company: 'PME B2B Services' },
                  { quote_fr: 'Méthodologie carrée. Mesure avant/après. On a su exactement ce qui marchait.', quote_en: 'Solid methodology. Before/after measurement. We knew exactly what worked.', author: 'Thomas R.', role_fr: 'CFO', role_en: 'CFO', company: 'ETI Industrie' },
                ],
                layout: 'grid-2',
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>GuaranteeBlock · type "guarantee-block" (3 garanties)</Kicker>
            <div class="ds-section-demo-frame">
              <GuaranteeBlock data={{
                type: 'guarantee-block',
                guarantees: [
                  { title_fr: 'Tu peux ramener le système en interne', title_en: 'Bring the system in-house', body_fr: 'Tout est documenté, ouvert, transférable. Pas de boîte noire, pas de lock-in.', body_en: 'Everything documented, open, transferable. No black box, no lock-in.' },
                  { title_fr: 'ROI mesuré avant après', title_en: 'Measured before/after ROI', body_fr: 'Baseline observée. Métriques cibles définies. Mesure post-déploiement.', body_en: 'Baseline observed. Target metrics defined. Post-deployment measurement.' },
                  { title_fr: 'Sortie sous 4 semaines si non-fit', title_en: 'Exit within 4 weeks if no-fit', body_fr: 'Si l\'audit révèle un non-match, on sort propre — pas de tunnel de plusieurs mois.', body_en: 'If audit reveals no-match, we exit clean — no months-long tunnel.' },
                ],
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>CtaInline · type "cta-inline" (CTA discret)</Kicker>
            <div class="ds-section-demo-frame">
              <CtaInline data={{
                type: 'cta-inline',
                text_fr: 'Tu veux pousser un projet pilote en 1 semaine ?',
                text_en: 'Want to push a pilot project in 1 week?',
                cta: { label_fr: 'Réserver →', label_en: 'Book →', href: '/contact', variant: 'primary' },
              }} lang="fr" />
            </div>
          </div>

          <div class="ds-section-demo">
            <Kicker tone="muted" size={10}>CtaFinal · type "cta-final" (bg paper)</Kicker>
            <div class="ds-section-demo-frame">
              <CtaFinal data={{
                type: 'cta-final',
                h2_fr: 'Prêt à <i>câbler</i> votre système ?',
                h2_en: 'Ready to <i>wire</i> your system?',
                body_fr: 'Audit en 5 jours. Premier workflow en production sous 1 semaine.',
                body_en: 'Audit in 5 days. First workflow in production in 1 week.',
                cta_primary: { label_fr: 'Réserver un audit →', label_en: 'Book an audit →', href: '/contact', variant: 'primary' },
                background: 'paper',
              }} lang="fr" />
            </div>
          </div>
        </div>
      </section>

      {/* ───────────── 7. EDITORIAL ───────────── */}
      <section id="editorial" class="ds-section">
        <ChapterLabel num="07" title="Editorial (8)" />
        <p class="ds-section-lede">
          Atomes éditoriaux pour MDX content (essais, livres-blancs, cas clients). Cf
          <code>src/components/editorial/</code>.
        </p>
        <ul class="ds-z-list">
          <li><strong>Callout</strong> · block d'avertissement ou note dans articles</li>
          <li><strong>PullQuote</strong> · citation breakout</li>
          <li><strong>DropCap</strong> · initiale magnifiée</li>
          <li><strong>KeyMetric</strong> · mise en valeur métrique inline</li>
          <li><strong>EditorialTable</strong> · tableau éditorial avec headers stylisés</li>
          <li><strong>Dingbat</strong> · séparateur ornemental</li>
          <li><strong>Sidenote</strong> (Tier 2) · note marginale rare</li>
          <li><strong>Timeline</strong> (Tier 2) · timeline narrative dans MDX (overlap TimelineBlock W6)</li>
        </ul>
      </section>

      {/* ───────────── 8. MOTION ───────────── */}
      <section id="motion" class="ds-section">
        <ChapterLabel num="08" title="Motion (4)" />
        <p class="ds-section-lede">
          Composants motion lourds — réservés pages signature (home, console, atlas).
          Cf <code>src/components/motion/</code>.
        </p>
        <ul class="ds-z-list">
          <li><strong>BootSplash</strong> · animation boot 1720ms · home FR/EN</li>
          <li><strong>LiquidHero</strong> (.tsx) · hero liquide canvas · console/atlas</li>
          <li><strong>AtlasConnections</strong> · graph connections · atlas</li>
          <li><strong>ProductReel</strong> (.tsx) · reel produit horizontal</li>
        </ul>
      </section>

      {/* ───────────── 9. TEMPLATES ───────────── */}
      <section id="templates" class="ds-section">
        <ChapterLabel num="09" title="Templates (18 — preview links)" />
        <p class="ds-section-lede">
          Templates pour collections content. Cf <code>src/components/templates/</code>. Preview live ci-dessous.
        </p>
        <ul class="ds-templates-list">
          <li><code>WelcomeTemplate</code> → <a href="/bienvenue/audit" class="ds-link">/bienvenue/audit</a></li>
          <li><code>UtilityTemplate</code> → <a href="/404" class="ds-link">/404</a></li>
          <li><code>TrustLegalTemplate</code> → <a href="/agence/trust-center" class="ds-link">/agence/trust-center</a></li>
          <li><code>CaseStudyTemplate</code> → <a href="/cas/plateau" class="ds-link">/cas/plateau</a></li>
          <li><code>ServiceDetailTemplate</code> → <a href="/offres/revops" class="ds-link">/offres/revops</a></li>
          <li><code>SolutionsDetailTemplate</code> → <a href="/solutions" class="ds-link">/solutions</a></li>
          <li><code>OffresDetailTemplate</code> → <a href="/offres" class="ds-link">/offres</a></li>
          <li><code>EssayTemplate</code> → <a href="/ressources/blog/brain-circuit" class="ds-link">/ressources/blog/brain-circuit</a></li>
          <li><code>HubTemplate</code> → <a href="/ressources" class="ds-link">/ressources</a></li>
          <li><code>ListIndexTemplate</code> → <a href="/cas" class="ds-link">/cas</a></li>
          <li><code>DetailMenuTemplate</code> → page menu détail</li>
          <li><code>TechnologiesDetailTemplate</code> → <a href="/technologies" class="ds-link">/technologies</a></li>
          <li><code>AuthorPageTemplate</code> → <a href="/equipe" class="ds-link">/equipe</a></li>
          <li><code>EcoleHubTemplate</code> → <a href="/ecole" class="ds-link">/ecole</a></li>
          <li><code>ConversionFunnelTemplate</code> → <a href="/offres/site-web-ia-landing" class="ds-link">/offres/site-web-ia-landing</a></li>
          <li><code>FormationDetailTemplate</code> → page formation</li>
          <li><code>TunnelStepTemplate</code> → tunnel acquisition</li>
          <li><code>LeadMagnetTemplate</code> → page lead magnet</li>
        </ul>
      </section>
    </main>
  </div>
</Base>

<style>
  /* ─── HEADER PAGE ─── */
  .ds-hd {
    padding-top: calc(var(--spacing-section, 96px) + var(--header-height));
    padding-bottom: clamp(40px, 5vw, 72px);
    border-bottom: 1px solid var(--hairline);
  }
  :global(.ds-kicker) { display: block; margin-bottom: 24px; }
  .ds-h1 {
    font-family: var(--font-display);
    font-size: clamp(48px, 9vw, 156px);
    line-height: 0.94;
    letter-spacing: -0.03em;
    font-weight: 400;
    text-wrap: balance;
    margin: 0;
    color: var(--ink);
  }
  .ds-h1 i { font-style: italic; color: var(--accent); }
  .ds-lede {
    font-family: var(--font-sans);
    font-size: clamp(18px, 1.8vw, 24px);
    color: var(--ink-2);
    margin: 32px 0 0;
    max-width: 720px;
    line-height: 1.5;
  }
  .ds-meta {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: .06em;
    color: var(--muted);
    margin: 24px 0 0;
  }
  .ds-meta code {
    background: var(--paper-2);
    padding: 2px 6px;
    border-radius: 4px;
  }

  /* ─── LAYOUT TOC + CONTENT ─── */
  .ds-layout {
    max-width: var(--wrap-wide);
    margin: 0 auto;
    padding: clamp(48px, 6vw, 80px) var(--gut);
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: clamp(40px, 5vw, 80px);
  }

  .ds-toc {
    position: relative;
  }
  .ds-toc-inner {
    position: sticky;
    top: calc(var(--header-height) + 24px);
  }
  :global(.ds-toc-label) { display: block; margin-bottom: 16px; }
  .ds-toc-list {
    list-style: none;
    margin: 0;
    padding: 0;
    border-left: 1px solid var(--hairline);
  }
  .ds-toc-list li {
    padding: 6px 0 6px 16px;
    border-left: 1px solid transparent;
    margin-left: -1px;
    transition: border-color var(--dur-fast) var(--ease);
  }
  .ds-toc-list li:hover { border-left-color: var(--accent); }
  .ds-toc-list a {
    font-family: var(--font-sans);
    font-size: 14px;
    color: var(--ink-2);
    text-decoration: none;
    line-height: 1.4;
  }
  .ds-toc-list a:hover { color: var(--accent); }

  /* ─── CONTENT SECTIONS ─── */
  .ds-content { min-width: 0; }
  .ds-section {
    margin-bottom: clamp(80px, 8vw, 120px);
    scroll-margin-top: calc(var(--header-height) + 32px);
  }
  .ds-section-lede {
    font-family: var(--font-sans);
    font-size: var(--text-body-md);
    color: var(--ink-2);
    line-height: 1.6;
    max-width: 64ch;
    margin: 16px 0 32px;
  }
  .ds-section-lede code,
  .ds-section-lede strong { font-weight: 600; }
  .ds-section-lede code,
  .ds-help code,
  .ds-meta code,
  .ds-class-name {
    font-family: var(--font-mono);
    font-size: 13px;
    background: var(--paper-2);
    padding: 2px 6px;
    border-radius: 4px;
  }

  /* ─── SUBSECTIONS ─── */
  .ds-subsection { margin: 40px 0; }
  .ds-h3 {
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--accent);
    margin: 0 0 16px;
    border-bottom: 1px solid var(--hairline);
    padding-bottom: 8px;
  }
  .ds-h4 {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    margin: 24px 0 12px;
  }
  .ds-help {
    font-family: var(--font-sans);
    font-size: 14px;
    color: var(--muted);
    line-height: 1.5;
    margin: 8px 0 16px;
    max-width: 64ch;
  }
  .ds-link {
    color: var(--accent);
    border-bottom: 1px solid currentColor;
    text-decoration: none;
  }

  /* ─── SWATCH GRID ─── */
  .ds-swatch-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 12px;
  }
  .ds-swatch-card {
    display: flex;
    gap: 12px;
    padding: 12px;
    border: 1px solid var(--hairline);
    background: var(--paper);
  }
  .ds-swatch {
    width: 56px;
    height: 56px;
    border: 1px solid var(--hairline);
    flex-shrink: 0;
  }
  .ds-swatch--paper { background: var(--paper); }
  .ds-swatch-info { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
  .ds-swatch-info code {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--ink);
  }
  .ds-swatch-hex {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
  }
  .ds-swatch-role {
    font-family: var(--font-sans);
    font-size: 11px;
    font-style: italic;
    color: var(--muted-2);
  }

  /* ─── TYPE STACKS + SCALE ─── */
  .ds-type-stacks {
    display: flex;
    flex-direction: column;
    gap: 24px;
    margin: 16px 0 32px;
    padding: 24px;
    background: var(--paper-2);
    border: 1px solid var(--hairline);
  }
  .ds-type-scale {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .ds-type-row {
    padding-block: 8px;
    border-bottom: 1px dashed var(--hairline);
  }

  /* ─── SPACING ─── */
  .ds-spacing-grid {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .ds-spacing-row {
    display: grid;
    grid-template-columns: 140px 1fr 80px;
    gap: 16px;
    align-items: center;
    font-family: var(--font-mono);
    font-size: 12px;
  }
  .ds-spacing-row code {
    color: var(--ink);
  }
  .ds-spacing-bar {
    height: 16px;
    background: var(--accent);
    display: inline-block;
  }
  .ds-spacing-val { color: var(--muted); text-align: right; }

  /* ─── SHADOWS ─── */
  .ds-shadow-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 24px;
    margin: 16px 0 24px;
  }
  .ds-shadow-card {
    padding: 24px;
    background: var(--paper);
    border-radius: 4px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    min-height: 100px;
  }

  /* ─── MOTION ─── */
  .ds-motion-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 12px;
    margin: 16px 0 24px;
  }
  .ds-motion-card {
    padding: 16px;
    background: var(--paper-2);
    border: 1px solid var(--hairline);
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .ds-durations {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 8px;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .ds-durations li {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--ink-2);
  }
  .ds-durations code,
  .ds-motion-card code {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink);
    background: transparent;
  }

  /* ─── Z-INDEX + LIST ─── */
  .ds-z-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .ds-z-list li {
    font-family: var(--font-sans);
    font-size: 14px;
    color: var(--ink-2);
    padding: 6px 12px;
    background: var(--paper-2);
    border-left: 2px solid var(--hairline);
  }
  .ds-z-list code {
    font-family: var(--font-mono);
    font-size: 12px;
    background: transparent;
    color: var(--ink);
  }

  /* ─── CLASS CARDS ─── */
  .ds-class-card {
    padding: 12px 16px;
    border: 1px solid var(--hairline);
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  /* ─── HAIRLINES DEMO ─── */
  .ds-hairlines {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .ds-hairlines div {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    margin-top: 16px;
  }
  .hline { border: 0; border-top: 1px solid var(--hairline); margin: 0; }
  .hline-strong { border: 0; border-top: 1px solid var(--hairline-strong); margin: 0; }

  /* ─── SECTIONS W6 LIST ─── */
  .ds-sections-list,
  .ds-templates-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 12px;
  }
  .ds-sections-list li,
  .ds-templates-list li {
    font-family: var(--font-sans);
    font-size: 13px;
    color: var(--ink-2);
    padding: 10px 14px;
    background: var(--paper-2);
    border: 1px solid var(--hairline);
    line-height: 1.4;
  }
  .ds-templates-list li code {
    font-family: var(--font-mono);
    font-size: 12px;
    background: transparent;
    color: var(--ink);
    margin-right: 8px;
  }

  /* ─── SECTION DEMOS ─── */
  .ds-section-demo {
    margin: 32px 0;
    padding: 0;
  }
  .ds-section-demo > :global(.u-kicker) { margin-bottom: 12px; display: block; }
  .ds-section-demo-frame {
    border: 1px dashed var(--hairline-strong);
    background: var(--paper);
    position: relative;
    isolation: isolate;
  }
  .ds-section-demo-frame::before {
    content: 'DEMO';
    position: absolute;
    top: 8px;
    right: 12px;
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.14em;
    color: var(--muted-2);
    z-index: 2;
  }
  /* Override header clearance pour heros en démo (sinon padding-top énorme dans le frame) */
  .ds-demo-hero :global(.hero-split),
  .ds-demo-hero :global(.hero-centered),
  .ds-demo-hero :global(.hero-fb) {
    padding-top: var(--section-base) !important;
  }

  /* ─── CODE BLOCK ─── */
  .ds-code {
    margin: 0;
    padding: 20px;
    background: var(--ink);
    color: var(--paper);
    overflow-x: auto;
    font-family: var(--font-mono);
    font-size: 12px;
    line-height: 1.5;
    border-radius: 4px;
  }
  .ds-code code { color: inherit; font-family: inherit; }

  /* ─── RESPONSIVE ─── */
  @media (max-width: 900px) {
    .ds-layout { grid-template-columns: 1fr; gap: 32px; }
    .ds-toc { display: none; }
  }
</style>
