---
---
<svg viewBox="0 0 800 24" preserveAspectRatio="xMidYMid meet"
  class="divider-hairline"
  aria-hidden="true">
  <!-- Ligne horizontale principale -->
  <line x1="0" y1="12" x2="800" y2="12" stroke="currentColor" stroke-width="0.6" vector-effect="non-scaling-stroke" opacity="0.22"/>
  <!-- Losange central -->
  <path d="M400 5 L407 12 L400 19 L393 12 Z" fill="none" stroke="currentColor" stroke-width="0.8" vector-effect="non-scaling-stroke" opacity="0.5"/>
  <!-- Dot terracotta dans le losange -->
  <circle cx="400" cy="12" r="1.5" fill="var(--waimia-accent, #C94F2E)"/>
  <!-- Dots symétriques -->
  <circle cx="360" cy="12" r="1" fill="currentColor" opacity="0.3"/>
  <circle cx="440" cy="12" r="1" fill="currentColor" opacity="0.3"/>
  <circle cx="320" cy="12" r="0.8" fill="currentColor" opacity="0.2"/>
  <circle cx="480" cy="12" r="0.8" fill="currentColor" opacity="0.2"/>
</svg>
<style>
  .divider-hairline {
    display: block;
    width: 100%;
    max-width: 100%;
    margin-inline: auto;
    color: var(--ink, #0c0b09);
  }
</style>
